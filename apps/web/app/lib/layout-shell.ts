/**
 * Shared max-width + horizontal padding for the top nav and primary page
 * columns (home, browse, EPG). Keeps TV/lean-back layouts from feeling boxed in
 * on large monitors while still readable on ultrawide.
 */
export const LAYOUT_CONTENT_CLASS =
  'mx-auto w-full max-w-[min(100%,1728px)] px-4 sm:px-6 lg:px-8';
